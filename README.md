# Size-Of-C
### A repository for my Size-Of-C APP in C. 👍
##### Compile the **`Main.c`** with **`gcc`** (command: **`gcc -fdiagnostics-color=always -g Main.c -o name-to-executable -Wall -Wextra -O3 -std=c23`**).
